[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
markers =
    live: hits the real Moveshelf API, deselect with '-m "not live"'
    browser: drives a real Chromium via playwright, needs 'playwright install chromium'
