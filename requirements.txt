# Deliberately minimal. Everything here ends up inside the executable, and a
# small binary is what keeps startup fast and antivirus quiet.

moveshelf-api>=1.6.3
requests>=2.31.0

# Only needed to build the .exe with build.py. Not bundled into it.
pyinstaller>=6.0.0
